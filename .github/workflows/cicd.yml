name: CICD

on:
  workflow_dispatch: 

  jobs:
    code:
    runs-on: ubuntu-latest
    steps:
      -name: "Clone the code"
       run: echo "Cloning thw code:
    build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
    - name: "Build using Docker"
    run: echo "Building using Docker"
    
    test:
      needs: [build]
      runs-on: ubuntu-latest
      steps:
      -name: "Running test cases"
      run: echo "Testing the app"
    
    deploy:
      needs: 
      runs-on: ubuntu-latest
      steps: 
      -name: "Deploying the case
      run: echo "Deployed the case"
